export type MovieDTO = {
  movie_title: string;
  release_date: string;
  genre: string;
  mpaa_rating: string;
  total_gross: string;
  inflation_adjusted_gross: string;
};

export type Movie = {
  movie_title: string;
  release_date: string;
  genre: string;
};
